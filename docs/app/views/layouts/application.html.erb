<!DOCTYPE html>
<html lang="en" class="scroll-smooth">
  <%= render "shared/head" %>

  <body>
    <div class="page">
      <%= render "shared/navigation" %>

      <main class="wrapper">
        <%= yield %>
      </main>
    </div>
  </body>
</html>
